# Binaries (anchored to repo root / bin so they don't match cmd/gateway-manager/)
/bin/
/gateway-manager

# Local dev data (SQLite DB, generated TLS cert)
/.localdata/

# Go
*.test
*.out
/vendor/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
